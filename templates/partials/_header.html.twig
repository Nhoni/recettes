<nav class="navbar navbar-expand-lg bg-light shadow-sm">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('home.index') }}">
            <img src="images/blog/logo.png" alt="logo C" style="height: 40px;">ookShare
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-between" id="navbarColor03">

            <!-- Recherche -->
            <div class="navbar-nav mx-auto">
                {% include 'components/_ search.html.twig' %}
            </div>
			
            <ul class="navbar-nav">
                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle text-dark" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            {{ app.user.fullname }}
                        </a>
                        <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                            {% if 'ROLE_ADMIN' in app.user.roles %}
                                <a class="dropdown-item" href="{{ path('admin') }}">Administration</a>
                                <div class="dropdown-divider"></div>
                            {% endif %}
                            <a class="dropdown-item" href="{{ path('user.edit', { id: app.user.id }) }}">Modifier mes informations</a>
                            <a class="dropdown-item" href="{{ path('user.edit.password', { id: app.user.id }) }}">Modifier mon mot de passe</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ path('security.logout') }}">Déconnexion</a>
                        </div>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-dark" href="{{ path('recipe.index') }}">Mes Recettes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-dark" href="{{ path('ingredient.index') }}">Mes Ingrédients</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link text-dark" href="{{ path('security.login') }}">Connexion</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-dark" href="{{ path('security.registration') }}">Inscription</a>
                    </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link text-dark" href="{{ path('contact.index') }}">Contact</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
